 ```
ALGORITIMO DOZE;
VAR
R, PI,RE : REAL
INICIO
R <- 1;
PI <- 3,14
ENQUANTO(R <= 20) 
 FAÇA 
  R <- R * R * R;
  R <- R * PI;
  R <- R * 3;
  RE <- R / 4;
  
  R <- R + 0.50;
IMPRIMA "O Volume é : ", RE;
fim
```

