#include <iostream>
#include <math.h>
using namespace std;

int main() {
   int vet[10];
   for(int i=0; i<10; i++) {
      cout << "Digite um número" << "\n";
      cin >> vet[i];
      if (vet[i] < 0) {
         vet[i] = vet[i] * -1;
      }
   }
   cout << v[];
}