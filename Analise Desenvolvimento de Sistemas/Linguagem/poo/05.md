---
tags: [aula, poo, codigo, cpp]
disciplina: "Linguagem"
tipo: aula
---

# 14

# 15

Construtor é uma função que inicializa a classe, por exemplo, existe ele com argumentos e ele por padrão.
Construtor() : id(0) nome("Construtor Padrão") {};

Destrutor é para limpar a memória pós execução

# 16

Escopo é aonde podemos usar variáveis.

void imprimir() {
int x;
cout << x;
}
cout << x; // Isso é um erro de escopo x não está acessível aqui
